@model WebMobileAssignment.Models.Parent
@{
    ViewBag.Title = "Parent Details";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="row mb-4">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center">
            <h5 class="mb-0"><i class="bi bi-people-fill"></i> Parent Details</h5>
            <a asp-action="ParentIndex" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Back to List
            </a>
        </div>
    </div>
</div>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>
        <strong>@TempData["ErrorMessage"]</strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="row">
    <div class="col-md-8 mx-auto">
        <div class="dashboard-card bg-white p-0 rounded shadow-sm overflow-hidden">
            <!-- Header Banner -->
            <div class="bg-light p-4 border-bottom text-center">
                <div class="display-1 text-secondary mb-2">
                    <i class="bi bi-person-circle"></i>
                </div>
                <h3 class="mb-0">@Model.User.FullName</h3>
                <span class="badge bg-danger mt-2">@Model.ParentId</span>
                <div class="mt-2">
                    <span class="badge bg-@(Model.User.IsActive ? "success" : "secondary")">
                        @(Model.User.Status ?? "active")
                    </span>
                </div>
            </div>

            <!-- Details Body -->
            <div class="p-4">
                <div class="row g-4">
                    <!-- Left Column: Contact & Personal -->
                    <div class="col-md-6 border-end">
                        <h6 class="text-uppercase text-muted small fw-bold mb-3"><i class="bi bi-person-badge"></i> Contact Information</h6>

                        <div class="mb-3">
                            <label class="small text-muted d-block">Email</label>
                            <strong>@Model.User.Email</strong>
                        </div>
                        <div class="mb-3">
                            <label class="small text-muted d-block">Phone Number</label>
                            <span>@(Model.PhoneNumber ?? "Not provided")</span>
                        </div>
                        <div class="mb-3">
                            <label class="small text-muted d-block">Address</label>
                            <span>@(Model.Address ?? "Not provided")</span>
                        </div>

                        <h6 class="text-uppercase text-muted small fw-bold mb-3 mt-4"><i class="bi bi-person-lines-fill"></i> Personal Details</h6>
                        <div class="mb-3">
                            <label class="small text-muted d-block">Date of Birth</label>
                            <span>@(Model.User.DateOfBirth?.ToString("dd MMM yyyy") ?? "Not provided")</span>
                        </div>
                        <div class="mb-3">
                            <label class="small text-muted d-block">Gender</label>
                            <span>@(Model.User.Gender ?? "Not specified")</span>
                        </div>
                        <div class="mb-3">
                            <label class="small text-muted d-block">Age</label>
                            <span>
                                @if (Model.User.DateOfBirth.HasValue)
                                {
                                    var age = DateTime.Now.Year - Model.User.DateOfBirth.Value.Year;
                                    if (DateTime.Now.DayOfYear < Model.User.DateOfBirth.Value.DayOfYear) age--;
                                    @age <text>years old</text>
                                }
                                else
                                {
                                    <text>Not available</text>
                                }
                            </span>
                        </div>
                    </div>

                    <!-- Right Column: Linked Students -->
                    <div class="col-md-6 ps-md-4">
                        <h6 class="text-uppercase text-muted small fw-bold mb-3"><i class="bi bi-people-fill"></i> Linked Students</h6>

                        @if (Model.Students != null && Model.Students.Any())
                        {
                            <ul class="list-unstyled">
                                @foreach (var student in Model.Students)
                                {
                                    <li class="mb-3">
                                        <div class="d-flex justify-content-between align-items-center">
                                            <div>
                                                <i class="bi bi-person-fill text-primary me-2"></i>
                                                <strong>@student.User.FullName</strong>
                                                <div class="text-muted small">@student.StudentId</div>
                                                @if (student.User.Email != null)
                                                {
                                                    <div class="text-muted small">@student.User.Email</div>
                                                }
                                            </div>
                                            <div class="btn-group">
                                                <a asp-controller="Admin" asp-action="StudentDetails" asp-route-id="@student.StudentId" class="btn btn-sm btn-outline-info" title="View Student">
                                                    <i class="bi bi-eye"></i>
                                                </a>
                                                <a asp-controller="Admin" asp-action="StudentEdit" asp-route-id="@student.StudentId" class="btn btn-sm btn-outline-warning" title="Edit Student">
                                                    <i class="bi bi-pencil"></i>
                                                </a>
                                            </div>
                                        </div>
                                    </li>
                                }
                            </ul>
                        }
                        else
                        {
                            <p class="text-muted">No linked students</p>
                        }

                        <h6 class="text-uppercase text-muted small fw-bold mb-3 mt-4"><i class="bi bi-info-circle"></i> Account Information</h6>
                        <div class="mb-3">
                            <label class="small text-muted d-block">Account Created</label>
                            <span>@Model.User.CreatedDate.ToString("dd MMM yyyy, hh:mm tt")</span>
                        </div>
                        <div class="mb-3">
                            <label class="small text-muted d-block">Account Status</label>
                            <span class="badge bg-@(Model.User.IsActive ? "success" : "secondary")">
                                @(Model.User.IsActive ? "Active" : "Inactive")
                            </span>
                        </div>
                    </div>
                </div>

                <!-- Additional Info -->
                <div class="row mt-4 pt-4 border-top">
                    <div class="col-12">
                        <h6 class="text-uppercase text-muted small fw-bold mb-3"><i class="bi bi-bar-chart"></i> Summary Statistics</h6>
                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label class="small text-muted d-block">Parent ID</label>
                                <strong>@Model.ParentId</strong>
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="small text-muted d-block">Children Linked</label>
                                <strong>@(Model.Students?.Count() ?? 0)</strong>
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="small text-muted d-block">Account Age</label>
                                <strong>@((DateTime.Now - Model.User.CreatedDate).Days) days</strong>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Footer Actions -->
            <div class="bg-light p-3 border-top text-end">
                <a asp-action="ParentEdit" asp-route-id="@Model.ParentId" class="btn btn-warning me-2">
                    <i class="bi bi-pencil"></i> Edit Parent
                </a>
                <a asp-action="ParentDelete" asp-route-id="@Model.ParentId" class="btn btn-danger">
                    <i class="bi bi-trash"></i> Delete Parent
                </a>
            </div>
        </div>
    </div>
</div>
